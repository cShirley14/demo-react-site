import { Center } from "@chakra-ui/react";
import { ProfileConstants } from "../DataDisplay/ProfileConstants";

export const DesktopUpgrade = () => {
  return (
    <Center width={"100%"} gap="6">
      <ProfileConstants variant={"unstyled"} />
    </Center>
  );
};
